/**
 * Credentials for the Prisma-seeded demo user (`npm run prisma` seed / `npx prisma db seed`).
 * Enable the login hint with NEXT_PUBLIC_SHOW_DEMO_LOGIN=true (e.g. on preview/staging).
 */
export const DEMO_TEST_EMAIL = "demo@venturevibe.app";
export const DEMO_TEST_PASSWORD = "DemoPass2026!";
